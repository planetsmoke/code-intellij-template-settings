public with sharing class ${NAME}
{
    public static Boolean skipTriggers = false;
    
    public static void run(){
        if (${NAME}.skipTriggers){
            return;
        }
        
        // before triggers
        
        
        // after triggers
        
    }
}